import * as gulp from "gulp";
import { dest, lastRun, parallel, registry, series, src, symlink, task, tree, watch } from "gulp";

export { src, dest, symlink, watch, task, series, parallel, registry, tree, lastRun };
export default gulp;
