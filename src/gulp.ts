import * as gulp from "gulp";

export const src: typeof gulp.src = gulp.src;
export const dest: typeof gulp.dest = gulp.dest;
export const symlink: typeof gulp.symlink = gulp.symlink;
export const watch: typeof gulp.watch = gulp.watch;
export const task: typeof gulp.task = gulp.task;
export const series: typeof gulp.series = gulp.series;
export const parallel: typeof gulp.parallel = gulp.parallel;
export const registry: typeof gulp.registry = gulp.registry;
export const tree: typeof gulp.tree = gulp.tree;
export const lastRun: typeof gulp.lastRun = gulp.lastRun;
